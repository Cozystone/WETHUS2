# 2026-03-19

- 사용자 요청: andolbot을 기존 cozystonebot과 별개 세션으로 분리 운영.
- 조치: `~/.openclaw/openclaw.json`에서 기존 top-level Telegram botToken을 `channels.telegram.accounts.andolbot`으로 이동, `defaultAccount`를 `cozystonebot`으로 명시.
- 검증: `openclaw status`에서 Telegram accounts 2/2 (`cozystonebot`, `andolbot`) 확인, 세션 키도 `agent:main:telegram:andolbot:...`로 분리 확인.
- 후속: andolbot은 allowlist(`7907425087`) 기반으로 제한 적용.

- 신규 제품 과제 수신: WENET(가상 연결/의미 기반 궁합 플랫폼) 설계 및 Flutter MVP 개발 착수.
- 1차 산출: docs 5종 + Flutter mock-first 스캐폴딩(도메인 모델, compatibility engine v1, explanation generator, facet-agent v0, 기본 화면 플로우) 생성.
- 사용자 결정: 2026-03-19 15:05 KST 기준 WENET 작업은 일단 보류. 이후 재개 예정.
- 신규 요청: TIMONEY 앱 착수(Windows 개발, iOS 스타일 UI). Flutter 기반 Cupertino 템플릿/탭 구조/Home-Ledger-Stats-Settings 기본 스캐폴딩 생성.
- 사용자 제품 방향 명시: 기본 UI 철학은 iOS 스타일이지만 Android도 동시 배포 대상.

- WETHUS2 UI/플로우 대규모 조정 진행:
  - 홈/탐색 미니요약 글라스 톤을 오렌지에서 뉴트럴 투명 계열로 교체.
  - explore_theme 배경 liquid 효과 실험 후 사용자 피드백으로 롤백, 이후 부분 재적용/재조정 반복.
  - 프로필 칩(이름+플랜) 및 드롭다운(요금제/프로필) 버튼에 liquid-metal 스타일 적용.
  - 네비게이션 배지(알림/햄버거) 위치 미세 조정 다수, 최종적으로 아이콘 기준 앵커링 및 즉시 갱신 로직 적용.

- Founder(프로젝트 시작하기) 제출 플로우 변경:
  - 제출 후 홈 이동 전 로더 표시(지속시간 수차례 튜닝 후 사용자 요청 기준으로 재조정).
  - 숨김 파일 input의 native `required`로 인해 제출 시 파일탐색기 팝업 발생하던 버그 수정(커스텀 검증만 사용).
  - 제출 완료 후 상단 토스트(글라스) 노출, 중복 토스트(페이지 이동 시 재호출) 방지 로직 추가.

- 프로젝트 심사/노출 정책 기능 추가:
  - AI 심사 결과에 따라 `approved/manual_review/rejected` 상태 처리.
  - 수동검수용 `admin.html` 추가(승인/거부 버튼, 작성 내용 확인).
  - 작성자 본인 프로젝트 수정/삭제 권한 추가(탐색 세부 모달 및 프로필에서 사용).
  - 탐색 세부 필터 확장(추천믹스/업로드순/과거순/인기순/내 프로젝트).
  - 승인된 본인 프로젝트를 해당 카테고리에서 상단 노출하도록 정렬 로직 반영.

- 북마크 기능 추가/정리:
  - 홈/탐색 카드에서 북마크 토글 가능, 프로필에 좋아요/북마크 활동 바 추가.
  - 카드/모달 북마크 아이콘을 무채색 인스타형 SVG로 통일.
  - 모달 북마크 첫 클릭 미반응 이슈 완화(이벤트 전파 차단).

- AI 심사 엔진 경로를 Gemini 중심에서 ChatGPT API 호출 경로(`askChatGPT`)로 전환.
  - OpenAI 키 미설정/호출 실패 시 자동 수동검수로 폴백하도록 처리.
