# 2026-06-26

- Added project-hub ops visibility for external integration activity logs and webhook readiness.
- Project hub now shows per-integration event summaries and whether webhook collection is configured.
- Added webhook issue/re-issue modal in `project-hub.html`.
- Backend `/integrations` payloads now sanitize demo tokens and stored webhook secrets from normal client reads.
- Replaced login-page Google widget bootstrap with backend-driven Google OAuth start/callback routes.
- New Google login flow is now server-owned for redirect, code exchange, session cookie issuance, and onboarding redirect.
- Refined project-hub activity log UX so external events render as readable Korean lines instead of raw webhook keys.
- Recent activity and progress timeline rows now show a consistent `text · time · source` format.
- Added a repo-root Vercel frontend production deploy helper because Git auto deploy skipped the latest frontend commit once.
- Added explicit stale-feed visibility on `opportunities.html` plus audit warnings when the published opportunity feed is older than 14 days.
- Added a leader-only webhook test event action in `project-hub.html` so activity-log ingestion can be verified from the hub without an external relay.
- Refreshed `data/opportunity-published.json` with active late-June and July 2026 opportunities so the public opportunity surface is no longer anchored to April data.
- Added `scripts/publish-opportunity-feed.js` plus ops docs so curated opportunity rows can be normalized, deduped, freshness-stamped, and re-published with one command.
- Fixed opportunity freshness timestamps to write explicit KST `updatedAt` values and clamp negative age displays in audits/UI.
- Added `scripts/smoke-google-oauth-login.js` and folded it into the commercial gate so Google login redirect/config regressions fail fast before release.
- Added `integration activity log` and `Google OAuth login` smokes to `.github/workflows/static-checks.yml` so ordinary push/PR CI catches those commercial regressions too, not only the heavier gate runner.
- Made `GOOGLE_LOGIN_REDIRECT_URI` explicit in source/config examples and added live `/auth/google/start` contract checks; live Render backend still needs redeploy to pick up the new login callback fallback.
- Expanded the commercialization checks so live `project-hub.html` must also keep the activity-log collection UI controls (`hubWebhookModal`, webhook action buttons, test-event handler) instead of only the timeline loader functions.
- Added a dedicated `.github/workflows/launch-readiness.yml` workflow that runs the strict launch-grade gate on schedule/manual dispatch and uploads readiness evidence artifacts for ops review.
- Added `--json` output to `print-production-rollout-status.js` and expanded launch-readiness artifacts to include machine-readable JSON evidence for future ops automation.
- Added a launch-readiness step-summary writer so GitHub Actions now shows the current launch verdict, drift state, and next actions directly in the workflow UI.
- Cleaned up local launch-readiness verification noise by ignoring generated evidence artifacts in deploy-source readiness and by making the step-summary reader tolerate BOM-prefixed JSON.
- Added `print-launch-readiness-snapshot.js` plus workflow artifacts so commercialization readiness and rollout state can be shared as one snapshot, not just separate raw reports.
- Extended deploy-source readiness ignores so generated `launch-readiness-snapshot.md/json` artifacts also do not show up as false local drift.
- Added a `Launch readiness artifact smoke` step to `static-checks.yml` so the snapshot/summary evidence scripts are executed in ordinary CI, not only in the dedicated launch workflow.
- Added `generate-launch-readiness-artifacts.js` so readiness evidence files are written in stable UTF-8 across shells instead of depending on redirect encoding behavior.
- Replaced the ad-hoc launch artifact CI command chain with `smoke-launch-readiness-artifacts.js` and wired that smoke into both `static-checks.yml` and `run-commercial-gate.js`.
- Updated `smoke-launch-readiness-artifacts.js` to clean up generated evidence files after verification so local worktrees stay clean.
- Expanded launch-readiness summary generation so the GitHub step summary can also be persisted as `launch-readiness-step-summary.md` inside the evidence artifact bundle.
- Added `export-launch-readiness-bundle.js` plus `launch-readiness-bundles/` ignore support so the current machine can export a timestamped evidence pack without dirtying the repo.
- Connected project-hub AI panels and AI chat to the backend `POST /ai/project-mentor` contract so structured mentor outputs persist in the hub instead of relying only on ad-hoc prompt text.
- Added `smoke-project-mentor.js` and included it in both static CI and the commercialization gate.
- Found and fixed a founder funnel blocker where submit still required a cover image even though the submit path already had a fallback image. The AI image prompt call now prefers the production API base instead of defaulting to localhost on live.
- Found and fixed a live project-hub latency bug where `hubApi()` probed localhost-style endpoints before the real Render backend, leaving AI/status panels stuck in loading on the live site.
- Polished project-hub mentor fallback wording so internal auto-refresh prompts no longer leak into user-facing mentor questions when the backend falls back.
- Added authoritative liked-project sync so likes now rehydrate from the backend after login/session restore and cloud sync, not only from the local tab state.
- Updated project application review responses and hub wiring so accepted/rejected applications immediately merge the latest project team state back into the project hub.
- Updated comment creation so the frontend immediately merges the backend-returned project after posting a comment, reducing local-only comment drift.
- Refreshed the shared frontend contract marker and launch-artifact smoke so frontend undeploys now show up clearly as live drift instead of being masked by a stale static contract tag.
- Improved rollout diagnostics so drift output now prints the local/live frontend contract marker values directly, making Vercel non-deploys obvious from one glance.
- Added explicit integration activity-log capability metadata to `/integrations/providers` and surfaced it in the project hub so launch truth is honest: Google Docs/Sheets log lifecycle events automatically, while external document-change events still require a relay/webhook forwarder.
- Updated backend drift/reporting scripts and STATUS so the commercialization gate now treats frontend drift as cleared and highlights the real remaining issue: Render backend lag on the new provider activity-log contract.
- Upgraded the project-hub webhook modal with copy buttons plus an Apps Script relay template/cURL sample so Google Docs/Sheets external-activity logging can be wired from the product UI instead of ad-hoc operator notes.
- Local checks passed:
  - `node scripts/validate-static.js`
  - `node scripts/run-commercial-gate.js`
  - backend boot smoke with `PORT=0`
- Next immediate step: commit, push, and verify live `login.html` plus `/auth/google/start` after deployment.
